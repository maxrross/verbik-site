# Verbik website

Standalone marketing site for Verbik, kept beside the native app in `../`.

```bash
pnpm install
pnpm dev
pnpm build
```

Production deployment is managed by the Vercel project connected to this repository. The site is intended to serve `https://verbik.app`.
